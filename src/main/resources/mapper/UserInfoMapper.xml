<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        <mapper namespace="com.newbee.poi.dao.UserInfoDao">

            <sql id="baseSQL"></sql>
    <!-- 根据id获取 -->
    <select id="queryAll" resultType="com.newbee.poi.entity.UserInfo">
        select * from t_user_info
    </select>

            <select id="queryByUserName" parameterType="java.lang.String" resultType="com.newbee.poi.entity.UserInfo">
                <include refid="baseSQL"/>
                <if test="userName!=null">
                    WHERE userName=#{userName,jdbcType=VARCHAR}
                </if>
            </select>
</mapper>